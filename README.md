This is a Bubble.io plugin called "🤌 Markdown Pro" that converts markdown to beautifully styled HTML with code highlighting support. The plugin also includes HTML-to-markdown conversion functionality via an external API.

It's a thin wrapper around `showdownJS`.
